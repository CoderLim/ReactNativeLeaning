import React, { Component } from 'react';
import {
  View,
  Modal,
  StyleSheet,
  Text,
} from 'react-native';

export default class ReleaseModal extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <Modal>
      </Modal>
    );
  }
}
